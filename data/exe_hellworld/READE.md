These files are built with mingw.

To install mingw:

```
brew install mingw-w64
```

To compile:

```
x86_64-w64-mingw32-g++
```

Runtime install:

```
brew install --cask wine-stable
```

Run:

```
wine [path]
```
